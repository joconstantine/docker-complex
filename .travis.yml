language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t joconstantine/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true joconstantine/react-test npm test -- --coverage

after_success:
  - docker build -t joconstantine/multi-client ./client
  - docker build -t joconstantine/multi-nginx ./nginx
  - docker build -t joconstantine/multi-server ./server
  - docker build -t joconstantine/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push joconstantine/multi-client
  - docker push joconstantine/multi-nginx
  - docker push joconstantine/multi-server
  - docker push joconstantine/multi-worker
